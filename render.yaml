services:
  - type: web
    name: data-analyst
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "./start.sh"
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: OCR_API_KEY
        sync: false
